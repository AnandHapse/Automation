var firstNAme = "Anand";
var lastName = "Hapse"; // Camel Case

// naming convention (cases)
// 1. Camel Case (standard for js variable and functions)

let useeName = "Camel Case";
let totalPrice = 99.99;
let isUserLoggedIn = true;

// 2. Pascal Case (standard for js classes and constructors)
// first letter of the first word is capital with "_" to separate the words.
let UserName = "Pascal Case";
let ShoppingCart = "Class name Style";
let IsUserLoggedIn = true;

// 3. Snake Case (standard for js variables and functions). basicallu used "_" to separate the words intesd of space.
let user_name = "Snake Case";
let total_price = 99.99;
let is_user_logged_in = flase;

// 4. Screaming Snake Case (standard for js constants). mostly used for constants.
// capital with "_" to separate the words.
const MAX_SIZE = 100;
const API_KEY = "abc23";
const DATABASE_URL = "https://api.example.com";

// 5. Hungarian Notation (prefix with type - olderstyle)

let strUserName = "String Prefix"; // string
let bActive = true;               // boolean
let nCount = 10;                  // number
let arrItems = [];                // array of items
let objUser = {};                 // object
let fnGetUser = function() {      // function
    return { name: "John", age: 30 };
}
let objUser = { name: "John", age: 30 }; // object
let fnGetUser = function() {      // function
    return { name: "John", age: 30 };
}
let objUser = { name: "John", age: 30 }; // object
let fnGetUser = function() {      // function
    return { name: "John", age: 30 };
}


// we will use only CAMEL Case & PAscal Cases or will use Screaming Snake Case for constants.
